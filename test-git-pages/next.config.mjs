/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/test-git-pages/src/app",
    output: 'export',
    images: {
        unoptimized: true,
      },
};

export default nextConfig;
